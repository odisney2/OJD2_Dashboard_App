import React from 'react';

const SunMoon = (props) => (
  <div className="main-content">
    <h2>{props.title}</h2>
    <p>This page will show when the sun and the moon rises and sets</p>
  </div>
);

export default SunMoon;